
<div id="app">

    <br />
    <br />
    <div class="card">
        <div class="card-header"><h2>User Permissions</h2></div>
        <div class="card-body">Manage permissions for user: <strong>{{model.userName}}</strong></div>
    </div>
    <br />
    <br />

</div>

@section Scripts
{


    <script>

      var app = new Vue({
            el: '#app',
            data:
            {
                model: {
                    userId: ""
                    ,userName: "Testman"
                    ,userRoles: []
                    ,userPermissions: []
                },
                idgen: 0
                ,roles: []
                ,permissions:[] 

            },
            methods: {

                markChanged: function (entry)
                {
                  
                },
                saveChanges: function ()
                {

                  
                    var context = this;
                    var invalid = false;

     
                    var baseurl = '@Url.Content("~/Model/API/")';
                    var endpointurl = baseurl + "xxx";


                    $.ajax({
                        url: endpointurl,
                        type: "POST",
                        contentType: "application/json",
                        data: JSON.stringify(list),
                        success: function (response)
                        {
                            context.model = response;
                            context.model.modelSaved = true;
                            setTimeout(function () { context.model.modelSaved = false; }, 3000);

                        },
                        error: function (response)
                        {
                            raiseErrorModal(response.responseJSON);
                        }
                    });

                }
               


            },
            computed:
            {
                example: function ()
                {

                }

            },
            mounted: function ()
            {

               

            }
        });


    </script>


}